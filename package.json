{
  "scripts": {
    "dev": "live-server frontend --port=3000 --open=/index.html",
    "deploy": "hardhat run scripts/deploy.js --network giwa-sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6",
    "live-server": "^1.2.2"
  }
}
